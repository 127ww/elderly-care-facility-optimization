# CLAUDE.md

This file provides guidance to Claude Code when working with code in this repository.

## 工作区用途

数学建模竞赛的建模、求解与论文写作。

论文写作规范详见 [PAPER.md](PAPER.md)，进入写论文阶段时请读取该文件。

## 流程锚点

用户给出新任务或切换方向时，先读 `step_tracker.md` 确认当前处于第几步、上一步产物是否齐全。若文件缺失，以对话中最近完成的操作（git commit 记录、输出文件）为准重建。

每完成标准流程中的一步，更新 `step_tracker.md`：写入步骤编号、完成状态、产物文件清单。格式：

```
## 当前进度：步骤 N
状态：完成 / 进行中
产物：
- ./xxx.py / ./findings.md / ...
下一步：步骤 N+1
```

此文件不依赖上下文记忆，对话再长也不会丢失进度。

## 技能调用硬性规则（闸门机制）

**铁律：`Skill` 工具调用是加载技能内容的唯一方式。口头说"使用 xx 技能"不等于 invoke。在对应闸门打开之前，禁止输出相关代码。**

| 闸门 ID | 你要写的代码包含 | 闸门（必须已出现在工具调用记录中） | 闸门未开时的唯一合法操作 |
|---------|----------------|--------------------------------|----------------------|
| G1 | `plt.` / `sns.` / `fig, ax` / `ax.` / `Figure` / `subplots` | `Skill("nature-skills:nature-figure")` | 调用 Skill 工具，不得写代码 |
| G2 | `to_excel` / `to_csv` / `pd.ExcelWriter` / `openpyxl` | `Skill("document-skills:xlsx")` | 调用 Skill 工具，不得写代码 |
| G3 | 任何实现功能或修复 bug 的代码（非纯研究/读文件） | `Skill("superpowers:test-driven-development")` | 调用 Skill 工具，不得写代码 |
| G4 | 分析/修复报错、异常、测试失败 | `Skill("superpowers:systematic-debugging")` | 调用 Skill 工具，不得写代码 |
| G5 | 新功能/新模块的设计与实现（非简单单行修改） | `Skill("superpowers:brainstorming")` | 调用 Skill 工具，不得写代码 |

**自查**：每次准备输出代码块之前，扫描代码内容 → 匹配闸门 ID → 检查对应 `Skill()` 调用是否已在本次会话的工具调用记录中 → 若未出现，先补调，禁止先写代码"等下再补"。同一 skill 整个 session 只需 invoke 一次。

## 标准流程

1. **审题** — 读完全文（PDF 用 markitdown MCP），列出数据清单、隐藏约束、不确定的假设及其合理性依据，确认后让用户拍板。列出约束清单初稿：逐一梳理题目给定的显式约束（等式/不等式/取值范围），并补充基于物理常识、业务逻辑的隐含约束（如非负、整数、互斥、量纲一致性等），形成"约束检查表"。
2. **superpowers:brainstorming** — 拆子题，每道 ≥2 种方案，对比精度/复杂度/数据需求/实现难度，排查量纲和边界条件。排查每个方案的约束完备性：确认方案所依赖的约束清单是否覆盖全部显式与隐含约束，是否存在求解器可能绕过的松弛空间。
3. **superpowers:writing-plans + planning-with-files:plan** — 方案落地为可执行步骤，建 task_plan.md / findings.md / progress.md，估算子题耗时。同步补全约束清单：将方案中新增或修正的约束条件纳入"约束检查表"。
4. **实现（编码自审点）** — **[自审：
 1. 随机种子是否已固定？所有文件路径是否均为相对路径？
 2. 本题是否具有标准规划形式（线性/整数/非线性）？
    → 若是，是否已优先考虑调用 Gurobi/LINGO 等专业求解器，而非手写粗糙启发式？
    → 若否，所选算法是否有充分的文献依据或对比实验支撑？
 3. 是否已枚举所有显式约束与隐含约束（量纲、边界、逻辑互斥等），并确认求解器不会因松弛/变换而绕过？]
    ** 先写公共模块（数据读取 → 清洗 → 特征工程/量纲统一 → 可视化），按子题递进，每题验算通过后 git commit 留档，大改前也 commit 确保可回滚；独立子任务用 superpowers:dispatching-parallel-agents 并行；报错用 superpowers:systematic-debugging；复杂算法 2 小时未收敛或计算量过大时，主动提启发式/简化替代方案，不卡死在单一思路上。
    **代码审查（自动触发）**：每个 q*.py 写完并运行通过后，发三 Agent 并行审查（逻辑/数值/风格），修正后再 git commit。
5. **验证（可视化自审点）** — **[自审：严格对照下方"图表多样性与可视化高级规范"，连续同类型图禁超3张，3D图强制刷白背景、防遮挡]** 约束逐项核验（硬性前置步骤）：在进入可视化/灵敏度分析之前，必须先将求解器输出的最优解逐一代回所有约束条件（含等式和不等式），列出"约束满足清单"，逐项标注紧约束/松约束状态，确认无绕路、无越界后方可继续。若发现任一约束不满足，立即回溯步骤 2 修正模型，严禁带着不可行解进入后续分析。
   核验通过后，执行：手动验算 + 边界测试 + 鲁棒性检验（数据加微小扰动/白噪声） + 灵敏度分析（关键参数 ±10%/±20%，配套可视化图表。**所有画图代码须先通过 G1 闸门**），结果不合理回到步骤 2。
6. **论文（排版自审点）** — 详见 [PAPER.md](PAPER.md)。摘要是论文之眼，须反复打磨。
   **写作规划矩阵（动笔前置）**：在写章节正文前，为每个核心建模与求解章节各建一行简表：
   | 章节 | 论证功能（一句话） | 关键数据来源（哪个 .py / 哪张表） | 回应的问题 |
   |------|-------------------|--------------------------------|-----------|
   | （按实际章节填入） | （该章要证明什么） | （代码输出 / 附件 / 文献） | （对应哪个子问题） |

   每行必须有具体数据来源列，禁止出现"引入模型""进行分析"等无数据锚点的行。填完后对照表格自审：是否有章节未被表格覆盖？是否有行缺少数据来源？表格应与 task_plan.md 中的子题拆解对齐。
   **[自审：
 1. 是否已按 PAPER.md"排版强制高亮规则"逐项核对：摘要核心数值与幅度、结论决策动作、图表分析极值/拐点、表格最优行整行加粗？
 2. 表格中数字/数学符号是否使用 \mathbf{}，正文变量是否使用 \mathbf{}，杜绝 \textbf{} 混入数学环境？
 3. 每个公式前是否有一句"动机陈述"？代码输出的数字是否已翻译为"人话"业务/物理决策方案？]
   ** LaTeX 排版（文中数值必须与代码输出一致，严禁编造），以 20-25 页为目标区间进行高质量扩充，严禁拼凑废话。**所有画图/表格代码须先通过 G1/G2 闸门**。
7. **收尾** — 支撑材料打包，全文校对（公式编号、图表引用、参考文献格式），按竞赛要求提交。
   **结构化审稿（定稿前自动触发）**：论文初稿完成后，单条消息发三个 Agent（无需等用户指令）：
   - **Agent A（数理逻辑）**：读论文 .tex + CLAUDE.md → 检查每个公式的符号是否与前文一致、约束是否完备、最优解是否满足所有约束、推导是否连续无跳跃。输出 `review_logic.md`。
   - **Agent B（数值一致性）**：读论文 .tex + 各 q*.py 输出 → 逐数字核对摘要 vs 正文 vs 表格 vs 代码输出，检查单位、百分比、绝对值的计算。输出 `review_numbers.md`。
   - **Agent C（写作规范）**：读论文 .tex + PAPER.md → 逐项检查排版强制高亮规则、语言多样性约束、碎片化用语、图表引用。输出 `review_style.md`。
   合并三份，逐条修正后定稿。

## 并行 Agent 自动触发规则

结构化审稿之外，三 Agent 并行模式可用于以下场景。到达触发条件时**自动发起，不等待用户指令**：

| 场景 | 触发条件 | Agent A | Agent B | Agent C |
|------|---------|---------|---------|---------|
| **建模方案评审** | 步骤 2 完成、方案已列出 | 数学正确性（公式推导/量纲/边界条件） | 算法可行性（复杂度/收敛性/参数敏感性） | 数据匹配度（假设是否被数据支撑） |
| **代码审查** | 任一 q*.py 写完并运行通过 | 逻辑正确性（bug/边界条件/索引越界） | 数值准确性（精度/溢出/收敛判据） | 风格一致性（命名/注释/路径/种子固定） |
| **数据清洗后验证** | 数据清洗脚本运行完毕 | 异常值未处理项 | 缺失值填补合理性 | 分布假设成立性（正态/线性/独立） |

（论文定稿审阅见步骤 7，不在矩阵中重复。）

**调用方式**：单条消息发三个 Agent 调用。每个 Agent 的 prompt 只包含其审查目标 + 待审查文件路径 + 相关 md（PAPER.md 或 CLAUDE.md），互不知晓对方存在。合并后逐条处理。

## 核心规则

*  默认 Python（numpy/scipy/cvxpy/scikit-learn），控制系统/Simulink/PDE 时可用 MATLAB。
*  **专业求解器：如果是线性规划/非线性规划问题，优先调用 LINGO 或 Gurobi 进行求解；如果是纯统计与检验，可声明使用 SPSS 或 statsmodels。**
- 固定随机种子，结果可复现
- 模型对比 ≥2 种，单模型无说服力
- 数据先清洗再建模，不跳过预处理
- 发现缺陷立刻指，不拖到最后
- 先方案后代码，用户确认方向再动手
- 代码变量名与论文符号一致，注释标注物理意义和单位
- 子题代码统一命名（q1_model.py, q2_optimize.py），最终生成 requirements.txt
- 文件读写一律相对路径（./data/, ./figures/），严禁绝对路径，确保评委可复现
- 数据清洗或长时间训练后，中间结果保存为 .csv/.pkl，后续步骤优先读缓存，禁止重复运行耗时模块
- 非题目给定的参数（物理常数、行业标准等）必须联网检索真实值，代码注释和论文中标注来源，严禁凭空编造

## 编码行为准则（源自 Karpathy 四项原则，英文原版 swarmclawai/andrej-karpathy-skills）

减少 LLM 常见编码错误的规范。根据项目需要与项目特定指令合并使用。

> 权衡：本规范偏向谨慎而非速度。简单任务可自行判断，不必逐条照搬。

### 1. 先想后写
不臆测，不隐藏困惑，主动暴露权衡。

编码前：
- 明确陈述你的假设。若不确信，先问。
- 若存在多种解读，全部列出——不要默默选一个。
- 若存在更简洁的方案，直说。理由充分时可以反驳用户。
- 若某事不清晰，停下来。指出具体困惑之处。发问。

### 2. 简洁优先
解决问题的最小代码量，不写推测性内容。

- 不添加未被要求的功能。
- 不对一次性代码创建抽象。
- 不添加未被要求的"灵活性"或"可配置性"。
- 不对不可能发生的场景做错误处理。
- 写了 200 行但 50 行就能搞定，重写。
- 自问："资深工程师会觉得这段代码过度设计吗？"若是，简化。

### 3. 精准修改
只改必须改的，只清理自己的改动带来的残留。

编辑已有代码时：
- 不"顺手改进"相邻代码、注释或格式。
- 不重构没有坏的东西。
- 匹配已有代码风格，即使与你偏好不同。
- 若发现无关的死代码，提出来——不要直接删。

当你的改动产生残留时：
- 移除因你改动才变得无用的 import / 变量 / 函数。
- 不要删除预先存在的死代码，除非被要求。

检验标准：每行改动都能直接追溯到用户的需求。

### 4. 目标驱动
定义成功标准，循环验证直至通过。

将任务转化为可验证目标：
- "添加验证" → "先为非法输入写测试，再让测试通过"
- "修 Bug" → "先写一个复现 Bug 的测试，再让它通过"
- "重构 X" → "确保重构前后测试全部通过"

多步骤任务先简述计划：
1. [步骤] → 验证：[检查项]
2. [步骤] → 验证：[检查项]
3. [步骤] → 验证：[检查项]

强成功标准让你独立闭环；弱标准（"让它能跑"）需要不断追问才能推进。

规范生效的标志：diff 中不必要的改动减少、过度设计导致重写的情况消失、澄清性问题出现在实现之前而非犯错之后。

## 图表多样性与可视化高级规范（拒绝单一图型）

- **【硬性去重约束】**：全文严禁连续出现 3 张以上同类型的图（如连续三张折线图）。必须根据建模阶段，强制调用以下【学术图表白名单】中的多元化可视化方案。

### 1. 数据预处理与统计描述阶段（严禁纯折线/柱状图）
- **分布与异常值展示**：优先使用 `sns.boxplot()`（箱线图）或 `sns.violinplot()`（小提琴图）展示数据区间与异常值。
- **特征相关性论证**：强制使用 `sns.heatmap(..., annot=True, cmap='coolwarm')`（相关性热力矩阵图）。
- **多变量联合分布**：使用 `sns.pairplot()`（散点图矩阵）或 `sns.jointplot(kind='kde')`（核密度估计联合分布图）。

### 2. 核心模型求解与算法演化阶段（必须体现动态与结构）
- **机理与拓扑结构**：若是网络流、路径规划、元胞自动机等问题，强制使用 `networkx` 绘制【网络拓扑图】或使用网格矩阵展示【状态演化空间图】。
- **智能算法收敛**：不仅画收敛曲线，多方案对比时优先采用 `Pareto前沿散点图`（多目标优化）或双轴图（同时展示目标函数值与约束满足度）。
- **物理/几何机理题**：必须调用 `plt.contour()`（等高线图）、`plt.streamplot()`（流线图）或 `ax.quiver()`（向量场/速度矢量图）来展示物理场变化。

### 3. 机器学习与模型评估阶段（强制标准量化图）
- **分类与预测任务**：必须绘制 `Confusion Matrix`（混淆矩阵热力图）、`ROC 曲线` 或 `P-R 曲线`。
- **回归与拟合任务**：必须绘制【残差拟合图】（Residual Plot，检查残差是否随机分布）和【Q-Q图】（检验残差正态性）。
- **特征贡献度度量**：使用横向条形图绘制【特征重要性排序图】（Feature Importance Bar），并用阴影标出置信区间。

### 4. 高维数据可视化与 nature-skill 联动规范

- **高维数据优先"降维投影"**：在展示灵敏度分析或多变量协同效应时，优先指导代码采用 2D 替代方案，这能完美契合 `nature-skills:nature-figure` 的扁平化美学：
  1. **等高线充填图 (`plt.contourf`)**：配合 `inline labels`（等高线数值标签），既清晰又极具学术高级感。
  2. **带有平滑插值的热力图 (`sns.heatmap` 或 `plt.imshow(..., interpolation='gaussian')`)**。

- **必须使用三维图时的"硬性微调"**：若物理机理或空间拓扑要求必须展示 3D 曲面（`ax.plot_surface`），须先通过 G1 闸门，然后在绘图脚本中强制植入以下结构美化代码：
  1. **消除塑料感线框**：强制设置 `edgecolor='none'`，让曲面色彩平滑过渡。
  2. **打破灰色方块盒**：必须通过以下代码将 3D 背景面板刷白，融入论文背景：
     `ax.xaxis.set_pane_color((1.0, 1.0, 1.0, 0.0))`
     `ax.yaxis.set_pane_color((1.0, 1.0, 1.0, 0.0))`
     `ax.zaxis.set_pane_color((1.0, 1.0, 1.0, 0.0))`
  3. **数据防遮挡约束**：必须显式调整视角 `ax.view_init(elev=20, azim=-60)`，或使用透明度 `alpha=0.85`，确保曲面的所有关键拐点、极值点在 2D 打印页面上清晰可见，绝不允许裸奔输出默认 3D 视图。

## 输出偏好

- 中文交流，精简输出：直接给结论/方案对比/代码，不逐行解释代码原理（除非用户提问）
*  大模型直接输出符合 `cumcmthesis` 宏包标准的 LaTeX 源码。
